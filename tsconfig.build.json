{
  "extends": "./tsconfig.json",

  // Config Env Variables SCO Backend FW Path: Path + Folder must equals tsconfig.build.json exclude path
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "src/controller", "controller"]
}
